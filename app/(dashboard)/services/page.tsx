import ComingSoon from '@/components/ComingSoon'

export const metadata = { title: 'Services' }

export default function ServicesPage() {
  return (
    <ComingSoon
      title="Services"
      phase="Phase 3 — sales & revenue engine"
      blurb="A service catalogue with pricing, plus a package builder (Silver / Gold / Platinum) that feeds proposals and auto-activates client work."
    />
  )
}
